# Docker Platform Configuration
# 设置目标平台：linux/amd64 (x86_64) 或 linux/arm64 (ARM)
# 如果不设置，默认为当前系统架构

# For AMD64/x86_64 (Intel/AMD processors)
DOCKER_PLATFORM=linux/amd64

# For ARM64 (Apple Silicon M1/M2, ARM servers)
# DOCKER_PLATFORM=linux/arm64

# Auto-detect (recommended)
# DOCKER_PLATFORM=
